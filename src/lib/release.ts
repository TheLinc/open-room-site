/** Open Room is in early development. While this is false the page asks for
 *  an email where the download would be; set it to true to bring the download
 *  back in the header, the hero and the get-started section. */
export const released = false;
