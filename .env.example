# Resend API key with Full access; Sending access can't create contacts.
# Server-only: never prefix it with NEXT_PUBLIC_. https://resend.com/api-keys
RESEND_API_KEY=
# Optional. A Resend segment id to add every signup to.
RESEND_SEGMENT_ID=
